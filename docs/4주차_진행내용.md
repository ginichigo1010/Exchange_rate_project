# 4주차 진행 내용 — 예린

---

## 개요

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-05-13 |
| 주제 | Attention-LSTM 통과 검증 + ALFA 논문 구조 분석 및 구현 |
| 실행 파일 | `15_attention_verify.py`, `16_alfa_compare.py` |

---

## STEP 1 — Attention-LSTM 통과 검증 (`15_attention_verify.py`)

### 배경

3주차에서 Attention-LSTM이 Test RMSE 9.07원으로 gate 통과했으나 마진이 **0.05원**으로 매우 작음.  
태현 선배님 4주차 분석(CHECK 2)에서 20개 시드 기준 세 시퀀스 모델이 사실상 동등하다는 결론이 나옴.  
→ 같은 조건(시드 0~19)으로 예린도 직접 검증.

### 검증 방법

- 시드: 0~19 (총 20개), 태현 선배님과 동일 조건
- 비교 모델: LSTM vs Attention-LSTM
- 평가 기준: gate 통과 여부 (Val·Test RMSE·MAE 모두 Zero Change보다 낮아야 통과)

### 결과

| 모델 | 통과 | 미통과 | 통과율 |
|------|------|--------|--------|
| LSTM | 14회 | 6회 | **70%** |
| Attention-LSTM | 18회 | 2회 | **90%** |

### 태현 선배님 결과와 비교

| 항목 | 태현 선배님 | 예린 |
|------|------------|------|
| 시드 수 | 20개 | 20개 |
| 평가 기준 | 세 모델 중 최저 RMSE (우승 횟수) | Zero Change gate 통과율 |
| LSTM 결과 | 6회 우승 (30%) | 14회 통과 (70%) |
| Attention 결과 | 6회 우승 (30%) | 18회 통과 (90%) |

**측정 기준이 달라서 결과가 다르게 보이는 것.**  
태현 선배님 데이터 기준 평균 RMSE 차이: **0.0006원** (통계적으로 유의하지 않음, p=0.665)  
→ gate 통과율 차이는 0.0006원짜리 차이가 binary(통과/미통과)로 증폭된 것.

### 결론

> **태현 선배님 결론과 동일: 세 시퀀스 모델은 사실상 동등하다.**

- gate 통과율 차이(90% vs 70%)는 통계적으로 유의하지 않음
- Attention 메커니즘이 평균 풀링과 동일하게 작동 (엔트로피 99.96%)
- Occam's razor 적용 시 가장 단순한 **LSTM이 합리적 선택**

---

## STEP 2 — ALFA 논문 분석

### 논문 정보

- **제목**: Prediction of foreign currency exchange rates using an attention-based long short-term memory network
- **저자**: Shahram Ghahremani, Uyen Trang Nguyen (York University, Canada)
- **출처**: Machine Learning with Applications 20 (2025) 100648
- **공식 코드**: https://github.com/shghahremani/Forex_Prediction_AttentionLSTM

### 우리 구현(13번 파일)과의 핵심 차이

| 항목 | 우리 구현 (Temporal Attention) | ALFA (Feature Attention) |
|------|-------------------------------|--------------------------|
| Attention 방향 | **시간 축** (어느 날이 중요?) | **피처 축** (어느 변수가 중요?) |
| LSTM 크기 | 32 units | **64 units** |
| Adam 옵티마이저 | 기본값 | **clipvalue=0.5, decay=0.001** |
| EarlyStopping | patience=20 | **patience=40** |

### ALFA Feature Attention 구조

```
입력 (batch, seq_len, num_features)
    ↓
LSTM(64, return_sequences=True)
    ↓  (batch, seq_len, 64)
FeatureAttention:
  - Dense(num_features, softmax) → attention 가중치
  - Dense(num_features, linear) → 피처 투영
  - element-wise 곱 → 가중합(axis=-1)
    ↓  (batch, seq_len)
Dense(1, linear)
    ↓
예측값
```

**핵심**: 각 시점에서 17개 피처 중 어떤 변수가 중요한지를 학습.  
우리 Temporal Attention은 10개 시점 중 어느 날이 중요한지를 학습.

---

## STEP 3 — ALFA 구현 및 비교 (`16_alfa_compare.py`)

### 비교 설계

- 시드 0~19 (20개) × 3개 모델 = **60번 학습**
- 동일 환경: raw_data_v2.csv, delta 타깃, window=10, 80/10/10 분할

| 모델 | 구조 | 특징 |
|------|------|------|
| LSTM | LSTM(32) → Dense(1) | 기존 베이스라인 |
| Temporal-Attn-LSTM | LSTM(32) + 시간 축 Attention | 우리 기존 구현 |
| ALFA | LSTM(64) + 피처 축 Attention | 논문 공식 구조 |

### 평가 지표

- gate 통과율 (Zero Change 기준)
- 평균 Test RMSE ± σ
- Test RMSE 분포 (boxplot)

> 실행 명령어: `python3 16_alfa_compare.py`  
> 출력 파일: `alfa_compare.png`

---

## 실험 환경

| 항목 | 내용 |
|------|------|
| 데이터 | raw_data_v2.csv (yfinance + FRED, 2015~2025) |
| 피처 수 | 17개 |
| 분할 | 시간순 8:1:1 (Train 2,272일 / Val 284일 / Test 285일) |
| 정규화 | MinMaxScaler, split-before-scale |
| 평가 단위 | 원(KRW), delta 예측 (역정규화 없음) |
| 실행 파일 | 15_attention_verify.py, 16_alfa_compare.py |
